import {
  ArrowRight,
  ArrowLeft,
  ArrowUp,
  ArrowDown,
  Space,
  moveRight,
  moveLeft,
  moveDown,
  rotate,
  drop,
  Reset,
  reset,
} from "./keyboard";
import {
  OCCUPY,
  LIBERATE,
  BATCH_LIBERATE,
  BATCH_OCCUPY,
  occupy,
  batchOccupy,
  liberate,
  batchLiberate,
} from "./Mesh";

export const keyboard = {
  ArrowRight,
  ArrowLeft,
  ArrowUp,
  ArrowDown,
  moveRight,
  moveLeft,
  moveDown,
  rotate,
  Space,
  drop,
  Reset,
  reset,
};

export const mesh = {
  OCCUPY,
  LIBERATE,
  BATCH_LIBERATE,
  BATCH_OCCUPY,
  occupy,
  batchOccupy,
  liberate,
  batchLiberate,
}
