import {
  ArrowRight,
  ArrowLeft,
  ArrowUp,
  ArrowDown,
  Space,
  moveRight,
  moveLeft,
  moveDown,
  rotate,
  drop,
} from "./keyboard";

export const keyboard = {
  ArrowRight,
  ArrowLeft,
  ArrowUp,
  ArrowDown,
  moveRight,
  moveLeft,
  moveDown,
  rotate,
  Space,
  drop,
};
